#include"main.h"
/**
 * print_alphabet_x10 - Entry point
 * Return: Always 0 (sucess)
 */
void print_alphabet_x10(void)
{
		char ch;

			for (ch = 'a' ; ch <= 'z' ; ch++)
			{
				_putchar(ch);
			}
				_putchar('\n');
}
